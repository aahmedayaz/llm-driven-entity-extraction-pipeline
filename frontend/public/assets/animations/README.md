# Celebration animations

CSS-driven graffiti celebration used by `JsonCelebrationModal`:

- Confetti particles (`celebration.css` → `confetti-fall`)
- Graffiti labels: Done!, Nice!, JSON
- Star spin and color blobs

Components live in `components/chat/animations/`.
